#include <stdio.h>
#include "function/decoration.c"

int main()
{
    border(default_border);
    add_space(15);
    printf("Inventory Management System\n");
    border(default_border);

    return 0;
}